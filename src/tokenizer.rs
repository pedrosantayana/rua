struct Tokenizer;

struct TokenStream(Vec<Token>);

struct 

impl Tokenizer {
    pub fn from_string(s: &str) -> Result<TokenStream, Error> {

    }
}

struct Token {
    token_type: TokenType,
    value: &str
}

enum TokenType {

}

#[test]
fn tokenizer() {
    let tokenizer = 
}